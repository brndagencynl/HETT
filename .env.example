# =============================================================================
# SHOPIFY STOREFRONT API (Required for production)
# =============================================================================
# Your Shopify store domain (without https://)
VITE_SHOPIFY_DOMAIN=hett-veranda.myshopify.com

# Storefront API access token
# Create in Shopify Admin → Settings → Apps → Develop apps
# Required scopes: 
#   - unauthenticated_read_product_listings
#   - unauthenticated_read_product_inventory
#   - unauthenticated_read_metaobjects (optional, for content blocks)
#   - unauthenticated_read_content
#   - unauthenticated_write_checkouts
#   - unauthenticated_read_checkouts
VITE_SHOPIFY_STOREFRONT_TOKEN=7b9c88c7d36a8956e672edfebc971aae

# Storefront API version (default: 2024-10)
VITE_SHOPIFY_API_VERSION=2024-10

# =============================================================================
# CONTENT MODE (Homepage blocks, footer, etc.)
# =============================================================================
# 'static' (default) - Use hardcoded frontend content, safe without metaobject scopes
# 'shopify_metaobjects' - Fetch from Shopify Metaobjects (requires scope)
VITE_CONTENT_MODE=static

# =============================================================================
# CONTENT PROVIDER (Blog posts)
# =============================================================================
# 'shopify' (default) - Use Shopify Storefront API
# 'mock' - Use mock data for development
VITE_CONTENT_PROVIDER=shopify

# =============================================================================
# GOOGLE ADDRESS VALIDATION API (for Vercel serverless function)
# =============================================================================
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Enable the "Address Validation API" in your Google Cloud project
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
